{
  "buildCommand": "cd artifacts/iica-website && npm install && npm run build",
  "outputDirectory": "artifacts/iica-website/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
