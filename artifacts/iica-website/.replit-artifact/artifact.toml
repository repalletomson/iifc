kind = "web"
previewPath = "/"
title = "IICA Website"
version = "1.0.0"
id = "artifacts/iica-website"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18682

[services.development]
run = "pnpm --filter @workspace/iica-website run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/iica-website", "run", "build" ]
publicDir = "artifacts/iica-website/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18682"
BASE_PATH = "/"
